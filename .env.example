# Telegram Bot Configuration
# ⚠️ 重要：变量名必须是 MMEMORY_BOT_TOKEN (不是 TELEGRAM_BOT_TOKEN)
# 从 @BotFather 获取 token: https://t.me/BotFather
MMEMORY_BOT_TOKEN=your_telegram_bot_token_here

# Database Configuration (可选，如果使用外部PostgreSQL)
# POSTGRES_PASSWORD=your_secure_password_here

# Application Configuration
ENVIRONMENT=production
LOG_LEVEL=info
LOG_FORMAT=json

# 时区设置
TZ=Asia/Shanghai

# AI Configuration (OpenAI)
# 是否启用AI功能（C3阶段需要启用以支持删除/暂停等自然语言操作）
MMEMORY_AI_ENABLED=true

# OpenAI API配置
# 从 https://platform.openai.com/api-keys 获取
MMEMORY_AI_OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OpenAI API Base URL (可选，使用代理或第三方服务时修改)
# MMEMORY_AI_OPENAI_BASE_URL=https://api.openai.com/v1

# OpenAI 模型配置
# 主要模型：推荐 gpt-4o-mini (性价比高) 或 gpt-4 (更智能)
# MMEMORY_AI_OPENAI_PRIMARY_MODEL=gpt-4o-mini

# 备用模型：必须与主模型一致以确保兼容性（C1阶段修复）
# MMEMORY_AI_OPENAI_BACKUP_MODEL=gpt-4o-mini

# OpenAI 调用参数
# Temperature: 0-2，越低越确定性，建议0.1
# MMEMORY_AI_OPENAI_TEMPERATURE=0.1

# 最大Token数：控制成本，建议1000
# MMEMORY_AI_OPENAI_MAX_TOKENS=1000

# 请求超时时间（秒）
# MMEMORY_AI_OPENAI_TIMEOUT=30s

# 最大重试次数
# MMEMORY_AI_OPENAI_MAX_RETRIES=3

# ============================================
# C3阶段功能说明
# ============================================
# 当前配置支持以下C3阶段新功能：
# 1. AI自然语言删除提醒（如："撤销今晚的健身提醒"）
# 2. AI暂停/恢复提醒（如："暂停一周的健身提醒"）
# 3. AI编辑提醒（如："把健身提醒改到晚上7点"）- 待实现
# 4. Once模式一次性提醒（如："明天下午3点提醒我取快递"）
# 5. 按钮式提醒管理（/list 命令显示删除/暂停按钮）
#
# 注意：启用AI功能需要设置 MMEMORY_AI_ENABLED=true 和有效的 API Key